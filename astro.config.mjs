import { defineConfig } from 'astro/config';

export default defineConfig({
  // If you deploy to GitHub Pages under a custom domain, set `site` to that
  // domain (e.g. 'https://www.valleylineplumbing.com') and drop a `public/CNAME`
  // file containing just the domain name. If you deploy to the default
  // https://<user>.github.io/<repo>/ path instead, also set `base: '/<repo>/'`.
  site: 'https://www.valleylineplumbing.com',
});
